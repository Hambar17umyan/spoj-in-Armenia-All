#include <iostream>
using namespace std;
typedef long long ll;

const int N = (1 << 10) + 5, M = 1002;
ll dp[N][M];
int main()
{
	int i, j, n, m;
	cin >> n >> m;
	//n<=10 & m<=1000
	ll mod = 1000000007;
	for (i = 0; i < m; i++)
	{
		for (j = 0; j < (1 << n); j++)
		{

		}
	}

	return 0;
}