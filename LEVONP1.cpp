#include <iostream>
#include <map>
#define ll long long
using namespace std;

map<ll, >>arr;
int main()
{
	ll i, j, k, m, n;	
	cin >> n >> k;
	for (i = 0; i < n; i++)
	{
		cin >> m;

	}
	for (i = 0; i < n; i++)
	{
		cin >> arr[i].first.second;
		arr[i].second = 1;
	}

	return 0;
}